import { ThemeColorPipe } from './theme-color.pipe';

describe('ThemeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
